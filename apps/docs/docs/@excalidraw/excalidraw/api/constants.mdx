# Constants

### FONT_FAMILY

**How to use**

```js
import { FONT_FAMILY } from "@drawink/drawink";
```

`FONT_FAMILY` contains all the font families used in `Drawink`. The default families are the following:

| Font Family | Description            |
| ----------- | ---------------------- |
| `Excalifont`    | The `Hand-drawn` font |
| `Nunito` | The `Normal` Font      |
| `Comic Shanns`  | The `Code` Font        |

Pre-selected family is `FONT_FAMILY.Excalifont`, unless it's overriden with `initialData.appState.currentItemFontFamily`.

### THEME

**How to use**

```js
import { THEME } from "@drawink/drawink";
```

`THEME` contains all the themes supported by `Drawink` as explained below

| Theme   | Description       |
| ------- | ----------------- |
| `LIGHT` | The `light` theme |
| `DARK`  | The `Dark` theme  |

Defaults to `THEME.LIGHT` unless passed in `initialData.appState.theme`

### MIME_TYPES

[`MIME_TYPES`](https://github.com/drawink/drawink/blob/master/packages/drawink/constants.ts#L101) contains all the mime types supported by `Drawink`.

**How to use **

```js
import { MIME_TYPES } from "@drawink/drawink";
```
