# ============================================================================
# Drawink Environment Configuration Template
# ============================================================================
# SIMPLE APPROACH:
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in your actual values in .env.local
# 3. Use the same .env.local for both development and production
#    (just change values as needed when deploying)
#
# .env.local is gitignored and should never be committed
# ============================================================================

# ----------------------------------------------------------------------------
# SERVER CONFIGURATION
# ----------------------------------------------------------------------------
PORT=3001
JSON_BACKEND_PORT=3001
NODE_ENV=development

# CORS Configuration (comma-separated origins)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:4321

# ----------------------------------------------------------------------------
# CLERK AUTHENTICATION
# ----------------------------------------------------------------------------
# Get these from: https://dashboard.clerk.com
# Frontend (public key - safe to expose)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Backend (secret key - keep private!)
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ----------------------------------------------------------------------------
# FIREBASE CONFIGURATION
# ----------------------------------------------------------------------------
# Get these from: Firebase Console > Project Settings > Your apps > Web app
# JSON format required by firebase.ts (all values are public - used in frontend)
VITE_APP_FIREBASE_CONFIG='{"apiKey":"","authDomain":"","projectId":"","storageBucket":"","messagingSenderId":"","appId":""}'

# Firebase Admin SDK (Backend only - for Firestore & Storage)
# Get from: Firebase Console > Project Settings > Service Accounts > Generate new private key
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# ----------------------------------------------------------------------------
# STRIPE BILLING
# ----------------------------------------------------------------------------
# Get these from: https://dashboard.stripe.com/test/apikeys
# Secret Key (Backend only - keep private!)
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Publishable Key (Frontend - safe to expose)
STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Webhook Secret (from Stripe Dashboard > Developers > Webhooks)
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Stripe Price IDs (from Stripe Dashboard > Products)
# Create products and copy the price IDs here
STRIPE_PRICE_ID_PRO=price_xxxxxxxxxxxxx
STRIPE_PRICE_ID_TEAM=price_xxxxxxxxxxxxx

# ----------------------------------------------------------------------------
# API CONFIGURATION
# ----------------------------------------------------------------------------
# URLs for tRPC API and WebSocket server
VITE_API_URL=http://localhost:3001
VITE_WS_URL=ws://localhost:3003

# Backend API URLs (for JSON data storage)
VITE_APP_BACKEND_V2_GET_URL=http://localhost:3001/api/v2/
VITE_APP_BACKEND_V2_POST_URL=http://localhost:3001/api/v2/post

# WebSocket server for real-time collaboration
VITE_APP_WS_SERVER_URL=http://localhost:3003

# AI Backend (optional)
VITE_APP_AI_BACKEND=http://localhost:3015

# Excalidraw Library (optional - can use official servers)
VITE_APP_LIBRARY_URL=https://libraries.excalidraw.com
VITE_APP_LIBRARY_BACKEND=https://us-central1-excalidraw-room-persistence.cloudfunctions.net/libraries

# ----------------------------------------------------------------------------
# APP CONFIGURATION
# ----------------------------------------------------------------------------
# Git SHA for version tracking (auto-filled during build)
VITE_APP_GIT_SHA=development

# PWA (Progressive Web App) configuration
VITE_APP_ENABLE_PWA=false
VITE_APP_DISABLE_PWA=true

# Disable browser "unsaved changes" warning in dev
VITE_APP_DISABLE_PREVENT_UNLOAD=true

# Enable analytics/tracking
VITE_APP_ENABLE_TRACKING=true

# Vite dev server port
VITE_APP_PORT=3000

# ----------------------------------------------------------------------------
# SENTRY ERROR TRACKING (Optional)
# ----------------------------------------------------------------------------
# Get from: https://sentry.io
VITE_SENTRY_DSN=

# ----------------------------------------------------------------------------
# DEBUG FLAGS (Development only)
# ----------------------------------------------------------------------------
# Show bounding boxes for text containers
VITE_APP_DEBUG_ENABLE_TEXT_CONTAINER_BOUNDING_BOX=false

# Collapse the debug overlay by default
VITE_APP_COLLAPSE_OVERLAY=true

# Enable ESLint in dev server
VITE_APP_ENABLE_ESLINT=true

# Disable live reload / HMR (useful for Service Worker debugging)
VITE_APP_DEV_DISABLE_LIVE_RELOAD=false

# Fast Refresh (React)
FAST_REFRESH=false

# ----------------------------------------------------------------------------
# NOTES
# ----------------------------------------------------------------------------
# Frontend variables (VITE_*) are exposed to the browser bundle
# Backend variables (without VITE_ prefix) are server-side only
# Never commit .env.local - it contains your secrets!
